# Server-side only. Never exposed to the browser.
ANTHROPIC_API_KEY=

# Required only if your API key is not scoped to a workspace.
ANTHROPIC_WORKSPACE_ID=

# Optional model override (defaults to claude-opus-5).
SONORA_MODEL=
